// Import the math's library from henqosft-maths
const maths = require('henqsoft-math');

// Use the math library to add
var result = maths.add(1, 3);

//Print the result of addition to the console
console.log(result);
